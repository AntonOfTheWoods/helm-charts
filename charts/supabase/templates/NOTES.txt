CHART NAME: {{ .Chart.Name }}
CHART VERSION: {{ .Chart.Version }}
APP VERSION: {{ .Chart.AppVersion }}

** Please be patient while the chart is being deployed **

The following elements have been deployed

{{- if .Values.auth.enabled }}
  - auth
{{- end }}
{{- if .Values.meta.enabled }}
  - meta
{{- end }}
{{- if .Values.realtime.enabled }}
  - realtime
{{- end }}
{{- if .Values.rest.enabled }}
  - rest
{{- end }}
{{- if .Values.storage.enabled }}
  - storage
{{- end }}
{{- if .Values.studio.enabled }}
  - studio
{{- end }}

Ingress (Gateway API) if enabled:
{{- if .Values.istio.enabled }}
  Gateway: {{ include "supabase.istio.ingress.namespace" . }}/{{ include "common.names.fullname" . }}-gateway (gateway.networking.k8s.io)
  HTTPRoute: {{ include "common.names.namespace" . }}/{{ include "common.names.fullname" . }}-api

Useful commands:
  kubectl get gateways.gateway.networking.k8s.io -n {{ include "supabase.istio.ingress.namespace" . }}
  kubectl describe gateway.gateway.networking.k8s.io {{ include "common.names.fullname" . }}-gateway -n {{ include "supabase.istio.ingress.namespace" . }}
  kubectl get httproutes.gateway.networking.k8s.io -n {{ include "common.names.namespace" . }}
{{- end }}
