{{- if .Values.istio.enabled }}
{{- /* Determine parent Gateway reference: either the one we create or an existing shared one */ -}}
{{- $autoGwName := printf "%s-gateway" (include "common.names.fullname" .) -}}
{{- $createdGwName := .Values.istio.gateway.name | default $autoGwName -}}
{{- $parentGwName := ternary $createdGwName (.Values.istio.gateway.existing.name) ( .Values.istio.gateway.create ) -}}
{{- $parentGwNamespace := (ternary (.Values.istio.gateway.namespace | default (include "supabase.istio.ingress.namespace" .)) (.Values.istio.gateway.existing.namespace) (.Values.istio.gateway.create)) -}}
{{- if and (not .Values.istio.gateway.create) (or (empty .Values.istio.gateway.existing.name) (empty .Values.istio.gateway.existing.namespace)) }}
{{- fail "istio.gateway.create is false; you must set istio.gateway.existing.name and istio.gateway.existing.namespace" }}
{{- end }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "common.names.fullname" . }}-api
  namespace: {{ include "common.names.namespace" . }}
  labels: {{- include "common.labels.standard" ( dict "customLabels" .Values.commonLabels "context" $ ) | nindent 4 }}
    app.kubernetes.io/part-of: supabase
    app.kubernetes.io/component: ingress
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ $parentGwName }}
      namespace: {{ $parentGwNamespace }}
  hostnames:
    {{- include "supabase.istio.validatedHosts" . | nindent 4 }}
    - {{ include "supabase.api.baseHost" . | quote }}
  rules:
    # functions (strip /functions/v1/) - explicit non-OPTIONS methods
    - matches:
        - path: { type: PathPrefix, value: /functions/v1/ }
          method: GET
        - path: { type: PathPrefix, value: /functions/v1/ }
          method: POST
        - path: { type: PathPrefix, value: /functions/v1/ }
          method: PUT
        - path: { type: PathPrefix, value: /functions/v1/ }
          method: PATCH
        - path: { type: PathPrefix, value: /functions/v1/ }
          method: DELETE
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.functions.fullname" . }}
          port: {{ .Values.functions.service.ports.http }}

    # auth fixed endpoints
    - matches:
        - path: { type: PathPrefix, value: /auth/v1/verify }
          method: GET
        - path: { type: PathPrefix, value: /auth/v1/verify }
          method: POST
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /verify
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.auth.fullname" . }}
          port: {{ .Values.auth.service.ports.http }}

    - matches:
        - path: { type: PathPrefix, value: /auth/v1/callback }
          method: GET
        - path: { type: PathPrefix, value: /auth/v1/callback }
          method: POST
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /callback
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.auth.fullname" . }}
          port: {{ .Values.auth.service.ports.http }}

    - matches:
        - path: { type: PathPrefix, value: /auth/v1/authorize }
          method: GET
        - path: { type: PathPrefix, value: /auth/v1/authorize }
          method: POST
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /authorize
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.auth.fullname" . }}
          port: {{ .Values.auth.service.ports.http }}

    # auth wildcard (strip /auth/v1/) - explicit non-OPTIONS methods
    - matches:
        - path: { type: PathPrefix, value: /auth/v1/ }
          method: GET
        - path: { type: PathPrefix, value: /auth/v1/ }
          method: POST
        - path: { type: PathPrefix, value: /auth/v1/ }
          method: PUT
        - path: { type: PathPrefix, value: /auth/v1/ }
          method: PATCH
        - path: { type: PathPrefix, value: /auth/v1/ }
          method: DELETE
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.auth.fullname" . }}
          port: {{ .Values.auth.service.ports.http }}

    # rest (strip /rest/v1/, remove headers) - explicit non-OPTIONS methods
    - matches:
        - path: { type: PathPrefix, value: /rest/v1/ }
          method: GET
        - path: { type: PathPrefix, value: /rest/v1/ }
          method: POST
        - path: { type: PathPrefix, value: /rest/v1/ }
          method: PUT
        - path: { type: PathPrefix, value: /rest/v1/ }
          method: PATCH
        - path: { type: PathPrefix, value: /rest/v1/ }
          method: DELETE
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
        - type: RequestHeaderModifier
          requestHeaderModifier:
            remove: ["apikey", "authorization"]
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.rest.fullname" . }}
          port: {{ .Values.rest.service.ports.http }}

    # realtime (rewrite to /socket) - explicit non-OPTIONS methods
    - matches:
        - path: { type: PathPrefix, value: /realtime/v1/ }
          method: GET
        - path: { type: PathPrefix, value: /realtime/v1/ }
          method: POST
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplaceFullPath
              replaceFullPath: /socket
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.realtime.fullname" . }}
          port: {{ .Values.realtime.service.ports.http }}

    # storage (strip /storage/v1/, add Forwarded header) - explicit non-OPTIONS methods
    - matches:
        - path: { type: PathPrefix, value: /storage/v1/ }
          method: GET
        - path: { type: PathPrefix, value: /storage/v1/ }
          method: POST
        - path: { type: PathPrefix, value: /storage/v1/ }
          method: PUT
        - path: { type: PathPrefix, value: /storage/v1/ }
          method: PATCH
        - path: { type: PathPrefix, value: /storage/v1/ }
          method: DELETE
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
        - type: RequestHeaderModifier
          requestHeaderModifier:
            add:
              - name: Forwarded
                value: "proto=https"
        - type: ResponseHeaderModifier
          responseHeaderModifier:
            set:
              - name: Access-Control-Allow-Origin
                value: "*"
              - name: Vary
                value: "Origin"
      backendRefs:
        - name: {{ include "supabase.storage.fullname" . }}
          port: {{ .Values.storage.service.ports.http }}

    # pg-meta (strip /pg/)
    - matches:
        - path:
            type: PathPrefix
            value: /pg/
      filters:
        - type: URLRewrite
          urlRewrite:
            path:
              type: ReplacePrefixMatch
              replacePrefixMatch: /
      backendRefs:
        - name: {{ include "supabase.meta.fullname" . }}
          port: {{ .Values.meta.service.ports.http }}
{{- end }}
